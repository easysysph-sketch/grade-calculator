# Grade Calculator (Python App)

Simple Python app that computes average grades and shows a pass/fail result.

## Features

- Handles multiple grade inputs
- Computes the average automatically while typing
- Shows a Passed or Failed result
- Uses 75 as the default passing grade
- Built using Python with AI-assisted development

## Purpose

This app helps students quickly calculate their average grades and check whether
they passed or failed.
